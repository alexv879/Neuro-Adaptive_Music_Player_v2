# Neuro-Adaptive Music Player v2 - Requirements
# Python 3.8+ required

# Core scientific computing
numpy>=1.21.0,<2.0.0
scipy>=1.7.0

# Deep learning (optional but recommended)
tensorflow>=2.10.0,<3.0.0
scikit-learn>=1.0.0

# Data loading and processing
pyedflib>=0.1.30  # EDF file loading (clinical EEG format)
h5py>=3.1.0       # HDF5 support for MATLAB files
mat73>=0.59       # MATLAB v7.3 file support
pandas>=1.3.0     # CSV/DataFrame handling
mne>=1.0.0        # Advanced EEG processing (optional)

# Music integration
pygame>=2.1.0     # Local audio playback
python-vlc>=3.0.0 # Alternative audio player
spotipy>=2.19.0   # Spotify API integration (optional)

# Serial/Bluetooth communication (for live EEG devices)
pyserial>=3.5     # Serial port communication
bleak>=0.14.0     # Bluetooth Low Energy support

# Visualization and monitoring
matplotlib>=3.4.0
seaborn>=0.11.0
plotly>=5.0.0     # Interactive plots (optional)

# Logging and utilities
tqdm>=4.62.0      # Progress bars
colorlog>=6.6.0   # Colored logging
python-dotenv>=0.19.0  # Environment variable loading

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0
pytest-timeout>=2.1.0

# Type checking (development)
mypy>=0.950
types-requests

# Code quality (development)
black>=22.0.0
flake8>=4.0.0
isort>=5.10.0

# Documentation (development)
sphinx>=4.5.0
sphinx-rtd-theme>=1.0.0

# Requirements for specific use cases:
# ======================================

# For DEAP dataset:
# pip install mat73 h5py

# For SEED dataset:
# pip install mat73 scipy

# For real-time EEG devices:
# pip install pyserial bleak

# For Spotify integration:
# pip install spotipy python-dotenv

# For advanced EEG analysis:
# pip install mne

# Minimal installation (no deep learning):
# pip install numpy scipy pandas pyedflib pygame

# Full installation:
# pip install -r requirements.txt

# Notes:
# ------
# 1. TensorFlow may require specific CUDA versions for GPU support
#    See: https://www.tensorflow.org/install/gpu
#
# 2. MNE-Python provides advanced EEG analysis but has many dependencies
#    Install separately if needed: pip install mne
#
# 3. For Windows users, some packages may require Visual C++ Build Tools
#
# 4. For Apple Silicon (M1/M2), use tensorflow-macos and tensorflow-metal
